version: "3"
services:
  app:
    build:
      context: app
      dockerfile: Dockerfile.production
    command: >
      bash -c "make test"
    volumes:
      - "./app:/app"
    image:
      vladislav1923/devops-for-programmers-project-lvl1
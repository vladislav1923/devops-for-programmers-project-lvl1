version: "3"
services:
  app:
    build:
      context: app
      dockerfile: Dockerfile
    command: >
      bash -c "make setup && make dev"
    volumes:
      - "./app:/app"
    ports:
      - "8080:8080"